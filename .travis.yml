language: node_js
node_js: "4"

branches: {only: [src]}

script: yarn build

cache:
  yarn: true
  directories:
    - node_modules

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: src/.vuepress/dist
  target_branch: master
  on:
    branch: src